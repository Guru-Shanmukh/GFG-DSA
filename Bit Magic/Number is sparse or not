class Solution {
  // Function to check if the number is sparse or not.
  public static boolean isSparse(int n) {
    // Check if any two adjacent bits are 1 using bitwise AND with a shifted version
    return (n & (n << 1)) == 0;
  }
}
